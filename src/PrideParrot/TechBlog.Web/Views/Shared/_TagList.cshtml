@model IList<Tag>

@foreach (var item in Model)
{
	<a asp-controller="Blog"
	   asp-action="Tag"
	   asp-route-slug="@item.UrlSlug"
	   title="@item.Name"
	   class="tag-cloud-link">
		@item.Name
	</a>
}